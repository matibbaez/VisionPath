---
---
<section id="home" class="relative min-h-[100dvh] w-full flex flex-col items-center justify-center overflow-hidden bg-black selection:bg-[--color-primary] selection:text-white">
    
    <div class="absolute inset-0 z-20 opacity-[0.05] pointer-events-none mix-blend-overlay" 
         style="background-image: url('https://grainy-gradients.vercel.app/noise.svg');">
    </div>

    <div class="absolute inset-0 z-0 perspective-grid opacity-40 md:opacity-30">
        <div class="grid-lines"></div>
    </div>

    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[350px] md:w-[1200px] h-[350px] md:h-[800px] bg-[--color-primary] opacity-25 md:opacity-15 blur-[90px] md:blur-[100px] rounded-full pointer-events-none z-0 animate-pulse-slow"></div>


    <div class="relative z-30 w-full px-4 flex flex-col items-center justify-center h-full">
        
        <div class="animate-fade-in-up inline-flex items-center gap-2 px-3 py-1 rounded-full border border-white/10 bg-white/5 backdrop-blur-md text-[10px] md:text-xs font-mono tracking-widest text-gray-400 mb-6 md:mb-10 hover:border-[--color-primary]/50 transition-colors cursor-default uppercase">
            <span class="relative flex h-2 w-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[--color-primary] opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2 w-2 bg-[--color-primary]"></span>
            </span>
            <span>Status: Online</span>
        </div>

        <div class="relative w-full max-w-[90rem] mx-auto text-center">
            <div class="grid place-items-center">
                <h1 class="col-start-1 row-start-1 font-black tracking-tighter leading-[0.9] text-6xl sm:text-8xl md:text-9xl lg:text-[11rem] xl:text-[13rem] text-transparent uppercase select-none pointer-events-none z-0 transform translate-y-2 md:translate-y-4 opacity-30"
                    style="-webkit-text-stroke: 1.5px rgba(255, 255, 255, 0.25);">
                    <span class="block">We Build</span>
                    <span class="block">The Future</span>
                </h1>
                <h1 class="col-start-1 row-start-1 font-black tracking-tighter leading-[0.9] text-6xl sm:text-8xl md:text-9xl lg:text-[11rem] xl:text-[13rem] text-white uppercase z-10 mix-blend-normal">
                    <span class="block animate-slide-up opacity-0" style="animation-delay: 0.1s;">We Build</span>
                    <span class="block text-glow-pop animate-slide-up opacity-0 pb-2 md:pb-8" style="animation-delay: 0.3s;">
                        The Future
                    </span>
                </h1>
            </div>
        </div>

        <p class="mt-8 md:mt-10 text-xl text-gray-400 max-w-[85%] md:max-w-2xl mx-auto font-light leading-relaxed text-center animate-fade-in opacity-0" style="animation-delay: 0.6s;">
            El futuro no se espera, <span class="text-white font-bold">se programa</span>.
            <br class="hidden md:block" />
            Convertimos tu visión en una <span class="text-white font-medium border-b border-[--color-primary]/30 pb-0.5">ventaja injusta</span> frente a tu competencia.
        </p>

        <div class="mt-8 md:mt-7 flex flex-col sm:flex-row gap-3 w-full sm:w-auto px-6 sm:px-0 animate-fade-in opacity-0" style="animation-delay: 0.8s;">
            <a href="#proyectos" class="group relative px-6 py-2.5 bg-white text-black font-bold text-sm rounded-full overflow-hidden transition-all hover:scale-105 w-full sm:w-auto text-center justify-center flex shadow-lg shadow-white/5">
                <div class="absolute inset-0 w-full h-full bg-gradient-to-r from-gray-200 via-white to-gray-200 opacity-0 group-hover:opacity-50 transition-opacity"></div>
                <span class="relative flex items-center gap-2">
                    Ver Trabajo
                    <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
                </span>
            </a>
            
            <a href="#contacto" class="px-6 py-2.5 rounded-full border border-white/20 text-white font-medium text-sm hover:bg-white/5 hover:border-white/40 transition-all backdrop-blur-sm w-full sm:w-auto text-center">
                Contactar
            </a>
        </div>
    </div>

    <div class="absolute bottom-1 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 animate-fade-in opacity-0" style="animation-delay: 1.2s;">
        <div class="flex flex-col items-center gap-2 animate-bounce opacity-50">
            <span class="text-[8px] md:text-[8px] uppercase tracking-[0.2em] text-white/70">Scroll</span>
            <div class="w-[1px] h-8 md:h-16 bg-gradient-to-b from-white via-white/50 to-transparent"></div>
        </div>
    </div>

    <div class="absolute bottom-0 left-0 w-full h-32 bg-gradient-to-t from-black via-black/80 to-transparent z-20 pointer-events-none"></div>

</section>

<style>
    /* KEYFRAMES (Sin cambios) */
    @keyframes slideUp {
        from { transform: translateY(30px) scale(0.95); opacity: 0; }
        to { transform: translateY(0) scale(1); opacity: 1; }
    }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    @keyframes pulseSlow {
        0%, 100% { opacity: 0.25; transform: translate(-50%, -50%) scale(1); }
        50% { opacity: 0.35; transform: translate(-50%, -50%) scale(1.1); }
    }
    .animate-slide-up { animation: slideUp 1s cubic-bezier(0.2, 1, 0.2, 1) forwards; }
    .animate-fade-in { animation: fadeIn 1.5s ease-out forwards; }
    .animate-pulse-slow { animation: pulseSlow 6s ease-in-out infinite; }
    
    .perspective-grid {
        transform: perspective(800px) rotateX(60deg) translateY(-50px) translateZ(-200px);
        overflow: hidden;
    }
    .grid-lines {
        width: 300%;
        height: 300%;
        background-image: linear-gradient(to right, rgba(255, 255, 255, 0.06) 1px, transparent 1px), linear-gradient(to bottom, rgba(255, 255, 255, 0.06) 1px, transparent 1px);
        background-size: 50px 50px;
        transform-origin: center;
        animation: moveGrid 20s linear infinite;
        mask-image: radial-gradient(circle at center, black 0%, transparent 70%);
        margin-left: -100%;
    }
    @keyframes moveGrid {
        0% { transform: translateY(0); }
        100% { transform: translateY(50px); }
    }
</style>